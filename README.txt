# To rebuild image and start Container (Only for the first time)
`docker-compose up --build -d`

# To remove Container
`docker-compose down`

# To start Container only (with out rebuild image - use old image)
`docker-compose up -d`

# To restart server only
`docker restart dgft-api`

# Khi sửa php file thì ko cần phải khởi động lại